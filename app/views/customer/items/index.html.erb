
<!--改行代わりの空白分-->
    <p class="text-center">　　　　　　　　　　　　</p>   
    <div class ="container">
        <!--<h2>ジャンル検索</h2>-->
            <div class="row-4">
                <div class="col">
                </div>
            </div>
            <div class="new-items">
                <button type="button" class="btn btn-outline-light">商品一覧</button>
                <!--改行代わりの空白分-->
              　　<p class="text-center">　　　　　　　　　　　　</p>
            </div>
    
            <div class="row-6">
                <div class="col d-flex flex-wrap wrap">
                <!--アイテム要素-->
                <% @items.each do |item| %>
                    <div class="items">
        				<div class="card">
        				    <%= link_to attachment_image_tag(item, :item_image ,size:'318x200') , item_path(item.id) %></br>
        				    <p class="text-center"><%= item.name %></p></br>
        				    <p class="text-center">¥<%= item.price.to_s(:delimited, delimiter: ',') %></p>
        				</div>
        			</div>
        			<% end %>
                </div>
            </div>
            <div class="row-2">
                <%= paginate @items %>
            </div>
    </div>
