<div class="container my-5">
  <div class="row">
    <div class-"offset-1">
      <h2 class="red-border px-3">注文履歴一覧</h2>
    </div>
  </div>

  <div class="row mt-3">
      <table class="table table-borderless">
        <thead>
          <tr class="red-border">
            <th>注文日</th>
            <th>配送先</th>
            <th>注文商品</th>
            <th>支払金額</th>
            <th>ステータス</th>
            <th>注文詳細</th>
          </tr>
        </thead>

        <tbody>
          <% @orders.each do |order| %>
            <tr class="brown-border">
              <td class="align-middle"><%= order.created_at.strftime("%Y-%m-%d %H:%M") %></td>

              <% shipping_address = ShippingAddress.find(order.shipping_address) %>
              <td class="align-middle">
                <%= "〒" + @order.shipping_postcode + "  " + @order.shipping_address + "  " + @order.shipping_name %>
                <%#= "〒" + shipping_address.postcode + "  " + shipping_address.address + "  " + shipping_address.last_name + shipping_address.first_name %>
              </td>

              <td class="align-middle">
                <% order.order_details.each do |f| %>
                <%= f.item_name %><br>
                <% end %>
              </td>

              <td class="align-middle"><%= order.total_price %></td>

              <td class="align-middle"><%= order.order_status %></td>

              <td class="align-middle"><%=link_to "表示する", order_path(order), class: "btn btn-sm btn-outline-danger" %></td>
            </tr>
          <% end %>
        </tbody>

      </table>
  </div>
</div>